# מערכת באדג'ים לקהילה - Homer Patuach

## סקירה כללית

מערכת באדג'ים מקיפה שמעניקה הכרה לחברי הקהילה על פעילותם ותרומתם.

## סוגי הבאדג'ים

### באדג'ים אוטומטיים (מבוססי פעילות)

1. **🌱 תורם** - מעל 5 פוסטים
2. **🌟 תורם על** - מעל 10 פוסטים
3. **💬 מגיב** - מעל 5 תגובות שניתנו
4. **🗣️ מגיב על** - מעל 20 תגובות שניתנו
5. **⭐ מדרג** - מעל 5 דירוגים (לייקים)
6. **🌠 מדרג על** - מעל 20 דירוגים (לייקים)

### באדג'ים מבוססי הרשאות

7. **✏️ עורך** - למשתמשים עם הרשאות עריכה (`edit_others_posts`)

### באדג'ים ידניים

8. **👑 מייסד** - ניתן על ידי מנהל באופן ידני

## תכונות המערכת

### 1. מעקב אוטומטי אחר פעילות

המערכת עוקבת אוטומטית אחר:
- **פוסטים שפורסמו** - ספירה אוטומטית של פוסטים מפורסמים
- **תגובות שניתנו** - מעקב אחר תגובות שהמשתמש כתב
- **דירוגים (לייקים)** - ספירת לייקים שהמשתמש נתן לפוסטים אחרים

### 2. Progress Bars

בעמוד הקהילה, משתמשים רואים:
- **באדג'ים שהושגו** - מוצגים בצבע מלא עם אנימציה
- **באדג'ים בתהליך** - מוצגים באפור עם:
  - Progress bar מונפש
  - טקסט מצב (למשל: "3 / 5")
  - אחוז השלמה ויזואלי

### 3. תצוגות שונות

#### עמוד הקהילה (למשתמש המחובר)
```php
hpg_display_user_badges_with_progress( $user_id )
```
- רשת של כל הבאדג'ים
- Progress bars לבאדג'ים שטרם הושגו
- אנימציות ואפקטים ויזואליים

#### עמוד פרופיל (לכל מבקר)
```php
hpg_display_earned_badges( $user_id )
```
- רק באדג'ים שהושגו
- תצוגה קומפקטית

#### כרטיס תורם (ליד פוסטים)
```php
hpg_display_earned_badges( $user_id, 3 )
```
- עד 3 באדג'ים ראשונים
- תצוגה מינימליסטית

## קבצים במערכת

### Backend (PHP)
- `includes/badges-system.php` - לוגיקה מרכזית של מערכת הבאדג'ים

### Frontend (CSS)
- `assets/css/badges.css` - עיצוב הבאדג'ים והאנימציות

### Integration
- `includes/community-page-template.php` - תצוגת באדג'ים בעמוד הקהילה
- `homer-patuach-grid.php` - טעינת נכסים

## פונקציות עיקריות

### בדיקת באדג'ים
```php
// בדיקה אם משתמש השיג באדג' מסוים
$has_badge = hpg_user_has_badge( $user_id, 'contributor' );

// קבלת כל הבאדג'ים שהמשתמש השיג
$earned_badges = hpg_get_user_badges( $user_id );

// קבלת התקדמות לבאדג' מסוים
$progress = hpg_get_badge_progress( $user_id, 'super_contributor' );
// Returns: ['current' => 7, 'required' => 10, 'percentage' => 70, 'earned' => false]
```

### ניהול באדג'ים ידניים (Admin)
```php
// מתן באדג' מייסד למשתמש
hpg_grant_manual_badge( $user_id, 'founder' );

// ביטול באדג' מייסד
hpg_revoke_manual_badge( $user_id, 'founder' );
```

## הוספת באדג' חדש

לדוגמה, הוספת באדג' "מנטור":

```php
// ב-includes/badges-system.php, בתוך hpg_get_badge_definitions()
'mentor' => [
    'name' => 'מנטור',
    'emoji' => '🎓',
    'description' => 'עזר ל-10 משתמשים חדשים',
    'threshold' => 10,
    'type' => 'mentorship_count', // שדה מטא חדש
    'manual' => false
]
```

ואז הוסף מעקב:
```php
function hpg_track_mentorship( $mentor_id ) {
    $current = get_user_meta( $mentor_id, 'hpg_mentorship_count', true );
    update_user_meta( $mentor_id, 'hpg_mentorship_count', (int)$current + 1 );
}
```

## אנימציות ואפקטים

### Badge Pulse
באדג'ים שהושגו מקבלים אנימציית דופק עדינה:
```css
@keyframes badgePulse {
    0%, 100% { transform: scale(1); }
    50% { transform: scale(1.1); }
}
```

### Progress Bar Shimmer
ה-progress bar כולל אפקט נוצץ:
```css
@keyframes shimmer {
    0% { transform: translateX(-100%); }
    100% { transform: translateX(100%); }
}
```

## Responsive Design

המערכת מותאמת למובייל:
- **Desktop**: רשת של 4-5 באדג'ים בשורה
- **Tablet** (< 768px): 3 באדג'ים בשורה
- **Mobile** (< 480px): 2 באדג'ים בשורה

## שדות Meta שנוצרים

המערכת יוצרת את שדות ה-meta הבאים:
- `hpg_total_posts_created` - סה"כ פוסטים
- `hpg_total_comments_given` - סה"כ תגובות שניתנו
- `hpg_total_ratings_given` - סה"כ דירוגים שניתנו
- `hpg_manual_badges` - מערך של באדג'ים ידניים

## Hooks זמינים

### Actions
- `hpg_user_liked_post` - כאשר משתמש נותן לייק
- `hpg_user_unliked_post` - כאשר משתמש מבטל לייק

### Filters
אין פילטרים ייעודיים כרגע, אבל ניתן להוסיף בעתיד.

## תאימות

- **WordPress**: 5.0+
- **PHP**: 7.4+
- **BuddyPress**: מומלץ (לא חובה)
- **ACF**: לא נדרש

## פיתוחים עתידיים

רעיונות להרחבה:
1. **לוח תוצאות** - דירוג משתמשים לפי מספר באדג'ים
2. **התראות** - הודעה כאשר משתמש משיג באדג' חדש
3. **באדג'ים נדירים** - באדג'ים מיוחדים לאירועים
4. **רמות** - מערכת רמות כוללת מבוססת נקודות
5. **תגמולים** - הטבות למשתמשים עם באדג'ים מסוימים

## תמיכה

לשאלות או בעיות, צור קשר עם צוות הפיתוח.
